on:
  push:
    branches:
      - master

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo "ref ${{ github.event.ref }}"
        echo "before ${{ github.event.before }}"
        echo "after ${{ github.event.after }}"
        echo "commits ${{ join(github.event.commits, ', ') }}"
