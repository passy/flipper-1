on:
  push:
    branches:
      - master

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo "env ${{ env }}"
        echo "job ${{ job }}"
        echo "github ${{ github }}"
        echo "event ${{ github.event }}"
